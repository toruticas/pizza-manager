const home = {
  key: 'home',
  path: '/',
  exact: true,
};

const dashboard = {
  key: 'dashboard',
  path: '/dashboard',
  exact: true,
};

const ordersNew = {
  key: 'orders-new',
  path: '/orders/new',
  exact: true,
};

const ordersStatus = {
  key: 'orders-status',
  path: '/orders/status',
  exact: true,
};

export { home, dashboard, ordersNew, ordersStatus };
